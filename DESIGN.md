# Design

slash의 시각 언어: **미니멀 베이스 + 마이크로그래픽 웹코어 + 브루탈 흔적**.
설계도/계기판처럼 정밀하고 정직한 **순수 흑백** 화면. 색은 앱 크롬이 아니라 **분류(카테고리)의 것** —
분류가 색을 가지며 그 색이 아바타 배경과 그 분류 메시지의 puffy 말풍선을 물들인다.

앵커 레퍼런스 (사용자 제공, 2026-07-15):
① 마이크로그래픽 다이어그램 — 가는 선, 모노스페이스 라벨(`[INDEX] 00030741`), 리더라인(`POWER ───── 3/100`), 기하 글리프(✳ ◯ ✕), 장식용 메타데이터
② BAT 채용 페이지 — 넓은 여백, 플랫 컬러 블록 CTA(보더·그림자 없음), **점선 구분선**, 보더 없는 회색 카드

## 원칙 (모든 UI 작업이 따를 것)

- **그림자 금지. 완전 플랫.** 누름 피드백은 색 변화(accent 살짝 어둡게 / surface 채움)로.
- **선은 1px 정밀선, 색은 `border` 토큰(연한 회색).** 진한(ink) 보더는 **선택 표시에만** 허용.
  두꺼운 보더(2px+) 금지. 라운드 0 유지 (브루탈 흔적).
- **면적 구분은 보더 없는 `surface` 카드로** (BAT 문법) — 설정 섹션·콘텐츠 그룹은 회색 면.
  아바타·채움 블록에는 보더를 두르지 않는다 (면으로 충분).
  **예외**: 분류 아바타(`CategoryAvatar`)는 흰/검 프로필이 배경에 묻히지 않게 1px `border` 링을 항상 두른다.
- **구분선은 점선(dotted)** — 콘텐츠 구분·리더라인·빈상태 박스는 dotted + textTertiary.
  패널 경계·헤더선은 실선 1px hairline (조용히).
- **모노스페이스 = 메타데이터의 목소리**: 타임스탬프·카운트·꼬리표(micro variant, **Azeret Mono** 번들).
- **분류**: 카테고리의 공식 명칭은 **"분류"** (EN Categories / JA カテゴリ). 이름은 접두어 없이 그대로 표기.
  각 분류는 **프로필(색)**을 가진다 — 사용자 노출 문구는 "색상"이 아니라 **"프로필"**.
  `CATEGORY_COLORS` 파스텔 프리셋 **8색**(핑크·노랑·연두·하늘·파랑·보라·검정·흰색) + **직접선택**
  (`components/ColorPicker` — 색조(0–360)·명도 **레일**을 끌거나 탭. 채도 65 고정·명도 30~88 클램프로 저대비 예방, 색조는 자유·파스텔 강제 없음).
  **레일은 색 견본 세그먼트의 나열(계기판식 색 표시)이지 장식 그라디언트가 아니다 — 그라디언트 금지의 예외가 아님.**
  직접선택한 색은 **"프로필 추가"(lucide `Plus`)로 스와치 그리드에 저장**할 수 있다(프리셋 다음, 최대 16개, 우상단 X 배지로 즉시 삭제·확인창 없음, 기본 8색은 삭제 불가). 저장 목록은 **서버 보관**(`users.customColors`, 웹·앱·데스크톱 공유). 스와치 삭제는 그 색을 이미 쓰는 분류엔 영향 없다(색은 `friend.color`에 박혀 있으니).
  목록 행 **long-press로 이름·프로필 수정**(편집기엔 삭제 버튼 없음 — 삭제는 행 스와이프 전용).
  아바타는 `components/CategoryAvatar` — 그 색 배경 위에 캐릭터(`charactor-transparent.png`, 박스의 ~70%)를 얹는다.
  분류 목록 행 오른쪽에 메시지 개수를 모노(micro)로 표시한다.
  편집은 채팅 헤더 이름 옆 **펜 아이콘**(lucide `Pencil`)으로도 연다 — 편집기는 루트 `CategoryEditProvider` 상주(어느 화면에서든·900px 교차에도 유지). **RN Modal이 아니라 절대위치 풀스크린 오버레이**(별도 네이티브 계층이 없어 RNGH 색 레일이 정상 동작).
  분류 탭은 **수동 순서**: 행 오른쪽 그립(lucide `GripVertical`) 드래그로 재배치, `position`으로 저장(드래그 중 표시는 surface 채움뿐 — 그림자 금지). 채팅 목록 방 정렬(고정→최신순)과는 무관.
- **행 스와이프 3액션** (분류·채팅 공통, `components/SwipeableRow`): 왼→오로 밀면 왼쪽에서 **[고정][삭제][수정]** 순서로 드러난다(각 ~68px, 플랫 블록·라운드 0·그림자 없음, lucide `Pin`/`PinOff`·`Trash2`·`Pencil`). 아이콘 전용이라 a11y 라벨 필수. 앱 크롬이 흑백이라 삭제 전용 색은 없다 — 세 블록 모두 ink 배경. 분류 탭은 그립(세로 드래그)과 방향 임계로 공존한다.
- **"전체"(자기 자신) 프로필**: 상단/전체 방도 분류처럼 **프로필 색**을 가진다(`users.selfColor`, 기본 검정 `SELF_DEFAULT_COLOR` `#1A1A1A`). 아바타는 `CategoryAvatar(selfColor)`, 편집은 채팅 전체 방 헤더 펜 → 편집기 self 모드(이름 없이 색만). 채팅 헤더는 뒤로가기·펜·검색 유무와 무관하게 **높이 고정**(minHeight)이라 전체 방·분류 방이 같은 크기다. 미분류 메시지 말풍선도 이 색으로 `makePuffy`(검정=회색 파스텔이라 기본 시각 변화 없음).
- **탭 헤더 공용**: 분류·채팅 탭 헤더(타이틀 + 우측 아이콘 버튼)는 `components/TabHeader`로 통일(복붙 금지). 두 탭 모두 +버튼으로 분류 추가.
- **아이콘은 lucide로 통일**: 네비(탭바·레일)는 아이콘만(라벨은 스크린리더용) —
  채팅 MessageSquare · 슬래시 Slash · 더보기 Ellipsis. 기존 MaterialCommunityIcons는 점진 교체.
- **글리프·메타 장식은 은은하게**: ✳ 마크, `SELF-MSG // 2026`, `─── 날짜 ───`, `라벨 ┄┄┄ 값` 리더라인.
  주변부(워드마크·빈상태·스탬프·섹션라벨)에만 — 본문·대화는 깨끗하게.
- **primary 버튼 = 보더 없는 플랫 ink 블록** (BAT CTA 문법). outline = 1px ink 박스. ghost = 밑줄 텍스트.
- **색**: 앱 크롬은 **순수 흑백 고정**(accent = ink). CTA(primary 버튼 · 전송 버튼 · 활성 탭 · 활성 선택 pill)도
  흑백 ink. 원색은 오직 **분류**에서만 등장한다 — 분류 아바타 배경 · 그 분류 말풍선. ink 위 글자는 `onAccent`(= inverse).
- **말풍선 = 푹신한(puffy) 재질** (`makePuffy(scheme, 분류색)`으로 파생; 미분류·색 없으면 회색 파스텔):
  파스텔 채움(`bubbleFill` L92) + 2px 틴트 테두리(`bubbleBorder`) + 진한 중립 글자(`onBubble`)
  + 윗변 빛띠·아랫변 그늘띠(인셋) + 틴트 그림자. 분류 방은 그 방 색 하나로 통일, 전체 방은 메시지마다 자기 분류 색.
  **라운드 예외: 말풍선 24 / 링크 카드 18** (그 외 모든 요소는 라운드 0). 다른 요소에 그라디언트·그림자 금지는 그대로.
- **대비**: 텍스트 WCAG 4.5:1 이상, 라이트·다크 모두.

## 구현 앵커 (수치·구현은 코드가 진실)

- 토큰: `frontend/src/theme.ts` — `makeColors`(라이트/다크 × accent), `brutal`(1px·offset 0), `typography`(micro=모노)
- 프레임: `components/Brutal.tsx` BrutalFrame (현재 = 1px 플랫 박스)
- **모든 모달·다이얼로그 = `components/ModalCard.tsx` 단일 문법**: 연한 스크림(`rgba(0,0,0,0.4)`,
  과한 검정 금지) + 중앙 카드(1px `border`·라운드 0·bg background·일관 패딩/폭 360) + 타이틀 +
  내용 슬롯 + **우측 정렬 공용 `Button` 액션**(ghost 취소 / primary 확인, destructive=outline).
  확인 다이얼로그(`Dialog`→notify/confirm), 분류·전체 프로필 편집(`category-edit`), 이름 편집(`name-edit`),
  메시지 액션 ⋮ 메뉴·태그 선택·내용 수정(`message-actions`), 태그 추가(`tag-create`)
  전부 이걸 쓴다 — 화면마다 카드·버튼을 따로 만들지 말 것. RN Modal이 아니라 절대위치 오버레이라
  카드 안의 RNGH(색 피커 레일)가 동작하고, **전부 루트 상주**라 데스크톱 3패널에서도 특정 패널이 아닌
  항상 전체 화면을 덮고 900px 교차에도 살아남는다.
  키보드 회피·작은 화면 스크롤은 ModalCard가 공통 처리.
- **폭 반응 규칙**: 콘텐츠 폭은 창 폭 판정이 아니라 **컨테이너 비례(%) + 절대 상한 캡**으로.
  (예: 말풍선 76% + 520px 캡, 링크 카드 76% + 460px 캡 — 스플리터 드래그에도 따라옴)
- 테마: `useTheme()` — 라이트/다크/시스템 모드 3택(AsyncStorage `slash.themeMode`). 색 취향은 없음(크롬 흑백 고정).
  분류 프로필색은 `CATEGORY_COLORS`(파스텔 프리셋 8) + 직접선택(`components/ColorPicker` 색조·명도 레일, 채도 65·명도 30~88 클램프) — 루트 오버레이 편집기에서 고르고, `makePuffy`가 말풍선 색으로 파생. "전체" 방 색은 `users.selfColor`(기본 `SELF_DEFAULT_COLOR`).
- 레이아웃: <900px 하단 3탭 · **전폭(폰 프레임 없음)** / ≥900px 3패널(레일 96 | 목록 — 드래그 스플리터 | 대화 상주).
  가운데 뜬 컬럼·폭 제한 프레임 금지.
- 웹 포커스: 키보드 탐색 시에만 currentColor 링 (제거 금지)

## 금지 목록

- 그림자 (soft든 hard-offset이든), 라운드 > 0, 두꺼운 보더(2px+), 알약 형태
- 그라디언트 (예외: 내 말풍선 하나), 글래스모피즘, 광택
- 시커먼 구조선 (2px ink), 본문/대화 영역에 장식 글리프 남발
- 본문·구분선 등 중립 요소에 색 (분류 아바타·분류 말풍선은 예외 — 분류색을 입는다)
- `Alert.alert`/`window.alert`/`window.confirm`, 하드코딩 색상
